import React from 'react'
import styles from './App.css'

export default function App() {
  return <div className={styles.root}>
    <h1>ts-loader demo</h1>
    <p><code>ts-loader</code> will look for files other than those bundled.</p>
  </div>
}
